def test_template_repo_py_importable() -> None:
    """Test that the package is importable.

    You can safely remove this unit test once implementing at least one other,
    though you should always test that the package is importable.
    """
    import template_repo_py  # isort:skip pylint: disable=import-outside-toplevel,unused-import
